name: Decision Engine Tests

on:
  push:
    branches: [ svc/decision ]
    paths:
      - 'app/engine/decision/**'
      - 'tests/decision/**'
      - '.github/workflows/decision-tests.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'app/engine/decision/**'
      - 'tests/decision/**'
      - '.github/workflows/decision-tests.yml'

jobs:
  test-decision:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'
        cache: 'pip'
        cache-dependency-path: |
          app/engine/requirements.txt
          app/engine/requirements-dev.txt

    - name: Install dependencies
      working-directory: app/engine
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt -r requirements-dev.txt

    - name: Run decision module tests
      run: |
        cd app/engine && PYTHONPATH=. pytest ../../tests/decision/ -v --tb=short --cov=decision --cov-report=xml --cov-report=term-missing

    - name: Check code formatting
      working-directory: app/engine
      run: |
        black --check decision/

    - name: Run linter
      working-directory: app/engine
      run: |
        ruff check decision/

    - name: Type check
      run: |
        python -m mypy app/engine/decision/*.py --ignore-missing-imports --follow-imports=silent

    - name: Upload coverage reports
      uses: codecov/codecov-action@v4
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        file: ./coverage.xml
        flags: decision
        name: decision-coverage
        fail_ci_if_error: false